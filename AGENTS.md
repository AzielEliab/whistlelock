# WhistleLock

Public identity: **Aziel Eliab**.

This is a local drop ledger + dead-man copy.
It is not a mailer, not an IP mask, not an inbox scraper.

Do not add private case facts. Demo drops are generic (`sample drop`).

Hosted `/v1` never stores drops or packets.

Forks are welcome and always allowed. Apache-2.0.
